/*
    변수(variable) : 값이 바뀔수 있는 것
    var[ES5] / let [ES6]

    상수(constant) : 한 번 할당한 값을 바꿀수 없는 것 [ES6]

    식별자(identifier) : 변수와 상수, 함수 이름
     - 규칙
       글자, $, _ 로 시작해야한다.
       * _ 또는 __ 은 아주 특별한 상황, 또는 '내부' 변수에만 사용한다.

    리터럴(literal) : 값을 프로그램 안에서 직접 지정한다는 의미, 값을 만드는 방법
     - 따옴표를 통해서 리터럴과 식별자를 구분한다. (식별자는 숫자로 시작할 수 없기 때문에 숫자는 따옴표가 필요없다.)






 */